[project]
name = "backpokia"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.127.1",
    "mistral>=21.0.0",
    "mistralai>=1.10.0",
]
